import React from "react";
import Tilt from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className="xs:w-[250px] w-full">
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className="w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card"
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className="bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col"
      >
        <img
          src={icon}
          alt="web-development"
          className="w-16 h-16 object-contain"
        />

        <h3 className="text-white text-[20px] font-bold text-center">
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className="mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]"
      >
       Software developer with more than 5 years of hands-on experience 
       designing and implementing efficient Java code and RESTful web apps with Spring, 
        Spring Boot, and Microservices, using modern design patterns and SDLC methodologies. S
        killed at back-end modules, and APIs into a fully functional system. Technical expertise 
        includes Java, Spring, Spring Boot, NodeJS, ExpressJs, REST, Microservice, MongoDB, Apache threejs, 
        CICD/Jenkins, AWS, Hystrix, Zipkins, Hibernate, JPA, MySQL, Angular, AWS, MSSQL, IntelliJ, Docker, 
        and Git. Always willing to learn and accept new challenges with the opportunity for professional 
        growth based on performance which will utilize my professional expertise while enabling me to expand
        my knowledge and skill set within a growing company!
      </motion.p>

      <div className="mt-20 flex flex-wrap gap-10">
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
